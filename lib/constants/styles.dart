import 'package:flutter/material.dart';

class AppStyles {
  static const EdgeInsets paddingContainer = EdgeInsets.all(12);
  static const EdgeInsets marginButton = EdgeInsets.only(top: 20);
  // Các hằng số khác
  static const double borderRadiusButton = 20.0;
}
